apiVersion: v1
kind: Service
metadata:
  name: grafana-service
spec:
  type: NodePort # será criado NodePort. Exposto externamente.
  selector:
    app: grafana
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 32000
